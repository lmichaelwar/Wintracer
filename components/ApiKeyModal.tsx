import React from 'react';

// This component is not used as the API key is configured via environment variables
// according to the security best practices.
export const ApiKeyModal: React.FC = () => {
    return null;
};
